version: '3'

# vars:
#   GREETING: Hello, world!

tasks:
  proto:
    desc: Protobuf generator
    cmds:
      - protoc --go_out=. --go_opt=paths=source_relative --go-grpc_out=. --go-grpc_opt=paths=source_relative api/proto/lab.proto